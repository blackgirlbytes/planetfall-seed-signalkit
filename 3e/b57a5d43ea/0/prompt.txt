Read the existing `signalkit` code first. Add a new feature: support reading the default request timeout from an environment variable `SIGNALKIT_TIMEOUT` (falling back to 10.0 if unset or invalid). Use this when the caller does not pass `timeout` explicitly to `fetch`. Add a `signalkit/config.py` helper `default_timeout()` and wire it into `fetch`. Add tests covering: env var set, env var unset, env var invalid. Keep retry behavior exactly as it currently is — do not change it. Use only the standard library. Run all tests, make sure they pass, then commit it yourself with git and a clear message about adding configurable timeout via SIGNALKIT_TIMEOUT.